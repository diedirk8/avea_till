<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <menuitem
        id="menu_avea_stock"
        name="Stock"
        parent="menu_avea_root"
        action="action_avea_stock_workspace"
        groups="avea_till.group_avea_manager"
        sequence="14"
    />

    <record id="menu_avea_stock" model="ir.ui.menu">
        <field name="action" ref="action_avea_stock_workspace"/>
    </record>

    <menuitem
        id="menu_avea_stock_products"
        name="Products"
        parent="menu_avea_stock"
        action="action_avea_stock_workspace"
        groups="avea_till.group_avea_manager"
        sequence="1"
    />

    <menuitem
        id="menu_avea_stock_receive"
        name="Receive Stock"
        parent="menu_avea_stock"
        action="action_avea_stock_receive"
        groups="avea_till.group_avea_manager"
        sequence="5"
    />

    <menuitem
        id="menu_avea_stock_count"
        name="Stock Take"
        parent="menu_avea_stock"
        action="action_avea_stock_take"
        groups="avea_till.group_avea_manager"
        sequence="8"
    />

    <menuitem
        id="menu_avea_stock_return"
        name="Return Stock"
        parent="menu_avea_stock"
        action="action_avea_stock_return"
        groups="avea_till.group_avea_manager"
        sequence="10"
    />

</odoo>
